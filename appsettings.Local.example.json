{
  "//": "Copy to appsettings.Local.json (git-ignored) and fill in, or set the same keys as environment variables (e.g. R2__SecretAccessKey). Never commit real values.",
  "Storage": {
    "Provider": "R2"
  },
  "R2": {
    "BucketName": "realworth-media",
    "Endpoint": "https://<account-id>.r2.cloudflarestorage.com",
    "PublicUrl": "https://pub-<id>.r2.dev",
    "AccessKeyId": "<from Cloudflare R2 API token>",
    "SecretAccessKey": "<from Cloudflare R2 API token>"
  }
}
